#include<iostream>
#include <climits>
using namespace std;

int main() {
    // Find smallest / largest in Array

    // int arr[] = {3,12,-2,55};
    // int size = sizeof(arr)/sizeof(arr[0]);
    // int small = INT_MAX; // we can also use arr[0] instead of INT_MAX
    // for(int i=0; i<size; i++){
    //     if(arr[i]<small){
    //         small = arr[i];
    //     }
        
    // }

    // cout<<small;


    // Largest value in Array

    // int arr[] = {4,3323,-2};

    // int size = sizeof(arr)/sizeof(arr[0]);

    // int large = 0; //we can also use INT_MIN; AND arr[0];

    // for(int i=0; i<size; i++){
    //     if(arr[i]>large){
    //         large = arr[i];
    //     }
    // }

    // cout<<large;



    return 0;
}